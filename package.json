{
  "name": "acala-tokens-subql",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "yarn build && ./node_modules/.bin/subql-node -f . --debug --timeout=512 --batch-size=100 --port=3123",
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "shenger",
  "license": "Apache-2.0",
  "devDependencies": {
    "@subql/cli": "^0.16.0",
    "@subql/types": "^0.10.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "@acala-network/sdk": "^4.0.2-12",
    "@acala-network/sdk-core": "^4.0.2-12",
    "@acala-network/sdk-wallet": "^4.0.2-12",
    "@acala-network/subql-utils": "^0.1.6-alpha.2",
    "@subql/node": "^0.28.0",
    "dayjs": "^1.10.7"
  }
}
