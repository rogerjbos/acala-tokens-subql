{
  "name": "acala-tokens-subql",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "./node_modules/.bin/subql-node -f . --timeout=512 --batch-size=2 --port=3123",
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "qwer951123",
  "license": "Apache-2.0",
  "devDependencies": {
    "@subql/cli": "^0.20.0",
    "@subql/types": "^0.10.1"
  },
  "dependencies": {
    "@acala-network/sdk": "^4.0.2-20",
    "@acala-network/sdk-core": "^4.0.2-20",
    "@acala-network/sdk-wallet": "^4.0.2-20",
    "@acala-network/subql-utils": "^0.1.17",
    "@subql/node": "^0.28.0"
  }
}
